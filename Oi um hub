-- Exemplo básico para Grow A Garden no Roblox

local player = game.Players.LocalPlayer
local replicatedStorage = game:GetService("ReplicatedStorage")

-- Supondo que tenha um evento remoto para vender frutas
local sellFruitEvent = replicatedStorage:WaitForChild("SellFruitEvent")

-- Função para manipular venda de frutas
sellFruitEvent.OnClientEvent:Connect(function(fruitType, amount)
    -- Aqui você intercepta o valor da venda e altera para 100000000
    -- Mas normalmente a venda é feita no servidor, então precisa manipular lá (server-side)
    print("Venda detectada: ", fruitType, amount)
    
    -- Esse é um exemplo, normalmente você não pode alterar o valor direto no client,
    -- o ideal é criar um script server-side para isso, se você tiver autorização.
end)

-- Duplica as sementes no inventário
local seedsFolder = player:WaitForChild("SeedsInventory") -- Ajuste o nome conforme seu jogo

for _, seed in pairs(seedsFolder:GetChildren()) do
    local clone = seed:Clone()
    clone.Parent = seedsFolder
end

print("Sementes duplicadas!")
